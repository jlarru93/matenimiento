spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${SECRETS_MAIL_USERNAME}
spring.mail.password=${SECRETS_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS}

spring.data.mongodb.database=${SECRETS_MONGO_DATABASE_NAME}
spring.data.mongodb.host=${MONGO_HOST}
spring.data.mongodb.port=${MONGO_PORT}
spring.data.mongodb.username=${SECRETS_MONGO_USERNAME}
spring.data.mongodb.password=${SECRETS_MONGO_PASSWORD}

spring.jackson.default-property-inclusion = NON_NULL
spring.jackson.time-zone=${TZ:America/Lima}

management.server.servlet.context-path=/actuator