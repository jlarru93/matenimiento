
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${SECRETS_MAIL_USERNAME:filipe.chaves.nt}
spring.mail.password=${SECRETS_MAIL_PASSWORD:1234!@#$}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
server.port=8083

spring.data.mongodb.database=${SECRETS_MONGO_DATABASE_NAME:email-service}
spring.data.mongodb.host=${MONGO_HOST:localhost}
spring.data.mongodb.port=${MONGO_PORT:27017}
spring.data.mongodb.username=${SECRETS_MONGO_USERNAME:adm}
spring.data.mongodb.password=${SECRETS_MONGO_PASSWORD:1234}

mongeez.enabled = true
mongeez.username=${SECRETS_MONGO_USERNAME:adm}
mongeez.password=${SECRETS_MONGO_PASSWORD:1234}


spring.jackson.default-property-inclusion = NON_NULL
spring.jackson.time-zone=${TZ:America/Lima}